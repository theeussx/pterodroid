# Porta principal do painel
PORT=3001

# Segredo para geração de tokens JWT (Troque para algo seguro!)
JWT_SECRET=pterodroid-secret-random-string-here

# Caminho para os dados (opcional, padrão é ./data)
# DATA_ROOT=./data

# Configurações de log
LOG_MAX_MEMORY=500
LOG_MAX_DB=1000
